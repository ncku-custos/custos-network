# Static — never edit this file. All per-host identity (mgmt IP, SSH user/key, NICs)
# lives in git-ignored host_vars/<host>.yml overlays: copy host_vars/*.yml.example,
# strip the suffix, fill in. Provision over a MANAGEMENT path, never the flight
# interface (the role restarts it and would cut a connection riding it).

[drone]
drone-01

[ground]
ground-01

[custos:children]
drone
ground
